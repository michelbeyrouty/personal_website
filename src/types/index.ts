export interface WorkExperience {
  title: string;
  company_name: string;
  logo: string;
  iconBg: string;
  date: string;
  points: string[];
}

export interface Service {
  title: string;
  icon: string;
  description: string;
}
